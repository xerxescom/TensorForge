# TensorForge 项目优化方案（Roadmap）

> 版本：v1.0  
> 日期：2026-04-17  
> 目标：在不改变现有对外功能的前提下，优先提升**性能稳定性、可观测性、可维护性**。

---

## 1. 优化目标与衡量指标

### 核心目标
- 降低基准测试的测量噪声（尤其是 LLM TTFT 与并发吞吐）。
- 缩短“非推理开销”占比（模型加载、缓存扫描、重复初始化）。
- 提升运行失败时的定位效率（结构化错误与报告聚合）。
- 增强配置系统的可控性和可验证性（复杂类型覆盖、校验报错）。

### 建议 KPI（可直接写入 CI 报告）
- **TTFT 波动系数（CV）**：较当前基线下降 ≥ 20%。
- **并发稳态吞吐**：在固定并发下提升 ≥ 10%。
- **缓存维护耗时**：大模型缓存场景下降 ≥ 50%。
- **错误归类覆盖率**：关键异常（下载/超时/OOM/依赖）覆盖率 ≥ 95%。

---

## 2. 优先级与实施清单

## P0（1~2 周，优先落地）

### 2.1 LLM TTFT 采样链路优化
**问题**：按字符读取 stdout 会引入额外系统调用与阻塞噪声。  
**方案**：
- 将逐字符轮询改为按行/块读取。
- 单独记录“首个非空 token 到达时间”，并把采样逻辑与日志输出解耦。
- 在报告中新增 `ttft_sampling_mode` 字段，标识采样方式（char/chunk/line）。

**收益**：TTFT 指标可信度更高，可重复性更好。

---

### 2.2 并发测试拆分冷启动与稳态
**问题**：基线 + 并发阶段重复模型初始化，吞吐指标掺杂大量加载时间。  
**方案**：
- 增加运行模式：`cold_start` / `steady_state`。
- 在 worker 进程中显式记录：`model_load_s`、`warmup_s`、`inference_only_s`。
- 报告分开展示“端到端吞吐”与“纯推理吞吐”。

**收益**：更接近真实线上压测口径，便于硬件横向比较。

---

### 2.3 模型缓存限额从“全量扫描”改为“元数据驱动”
**问题**：每次限额清理都递归扫描目录，缓存规模扩大后耗时明显。  
**方案**：
- 引入缓存元数据文件（JSON/SQLite 均可），记录模型大小、最近访问时间、来源。
- 下载成功后增量更新元数据；清理时先用元数据排序，再按需校验。
- 对异常场景保留 fallback 全量扫描。

**收益**：降低 housekeeping 开销，提升下载与测试启动速度。

---

## P1（2~4 周）

### 2.4 采样统计内存/CPU 优化
**问题**：全量 list + sort 计算分位数，长时间运行成本高。  
**方案**：
- 对 mean/min/max 使用在线算法。
- 分位数采用近似算法（如 t-digest）或窗口聚合。
- 提供 `stats_precision_mode`（exact / approximate）切换。

**收益**：长测时内存更稳，统计耗时更低。

---

### 2.5 CLI 配置覆盖能力增强
**问题**：`--set` 对 list/dict 类型支持弱，易出现隐式字符串错误。  
**方案**：
- 增加 `json:` 前缀覆盖（如 `--set llm_prompts=json:["a","b"]`）。
- 增加 `--set-file` 支持复杂结构注入。
- 对关键字段增加类型校验与友好报错。

**收益**：降低误配置概率，提升自动化脚本可维护性。

---

### 2.6 CV benchmark 参数语义一致化
**问题**：公开了 `batch_size` 参数，但执行逻辑未真正批量推理。  
**方案**：
- 实现真实 batch 输入与统计；或移除参数避免误导。
- 在报告写入 `effective_batch_size`。

**收益**：报告解释性提升，避免参数与行为不一致。

---

## P2（持续改进）

### 2.7 错误可观测性升级
- 统一错误结构：`error_type` / `error_stage` / `short_trace`。
- 汇总报告增加错误分布图（下载失败、OOM、超时、依赖缺失）。
- 输出可直接用于告警系统的 machine-readable 字段。

### 2.8 评测指标标准化
- ASR 指标补充标准 WER（S/D/I）实现，并保留近似值作为兼容字段。
- 报告中标注指标定义和计算方式版本号。

---

## 3. 推荐里程碑

- **M1（第 1 周）**：TTFT 采样改造 + 并发模式拆分（冷启动/稳态）。
- **M2（第 2~3 周）**：缓存元数据化 + 报告字段扩展。
- **M3（第 4 周）**：统计优化 + 配置覆盖增强 + CV 参数一致化。
- **M4（第 5 周+）**：错误治理与评测标准化持续迭代。

---

## 4. 风险与回滚策略

### 主要风险
- 指标口径变化导致与历史数据不可直接对比。
- 稳态模式引入后，用户可能误读冷启动时延。
- 元数据与真实磁盘状态可能出现短暂不一致。

### 控制策略
- 报告保留 `metric_version` 与 `run_mode` 字段。
- 新旧逻辑保留 1 个小版本并行输出（兼容窗口）。
- 元数据异常时自动 fallback 全量扫描并自愈。

---

## 5. 交付物建议（每阶段）

- 代码变更 + 单元测试 + 关键场景回归脚本。
- `final_report.json` 字段变更说明（changelog）。
- 一份 1 页以内的“本阶段优化收益复盘”（耗时、吞吐、失败率对比）。

---

## 6. 结论

该优化路线遵循“**先提升指标可信度，再提升执行效率，最后完善工程体验**”的原则：
1) 先解决 TTFT 与并发口径问题，确保数据可信；
2) 再降低缓存与统计链路开销，提升运行效率；
3) 最后补齐配置和错误治理，提高可维护性与可扩展性。

如果你希望，我可以继续基于这个文档输出一个 **Phase-1 的具体改造任务清单（按文件级别拆分）**，便于直接进入开发。
