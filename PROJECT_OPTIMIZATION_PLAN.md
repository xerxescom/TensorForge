# TensorForge 项目优化方案（执行版）

> 日期：2026-04-17  
> 目标：在不影响现有评测结果可比性的前提下，先提升“稳定性 + 可解释性 + 执行效率”，再推进“架构和可维护性”优化。

---

## 1. 总体优化目标

- **结果更可信**：减少采样噪声与偶发抖动，保证同配置可复现。
- **运行更高效**：缩短测试总时长，减少重复下载与重复加载。
- **成本更可控**：降低缓存与采样带来的 CPU/内存开销。
- **诊断更清晰**：失败原因结构化，报告可直接定位瓶颈。

---

## 2. 优先级路线图（建议按阶段落地）

### Phase 1（1~2 周，低风险高收益）

1. **LLM TTFT 采样方式优化**
   - 问题：按字符读取 stdout 容易引入额外开销与测量噪声。
   - 方案：改为按块/按行读取，记录首个有效输出时间戳；保留原字段并新增 `ttft_method` 说明。
   - 验收：同模型 10 次重复测试的 TTFT 标准差下降。

2. **并发测试拆分“加载时间”和“推理时间”**
   - 问题：并发压测中模型初始化时间会污染吞吐指标。
   - 方案：统一记录 `model_load_s`、`warmup_s`、`inference_only_s`，报告中分别展示。
   - 验收：报告可直接区分“冷启动瓶颈”与“稳态瓶颈”。

3. **缓存体积统计增量化**
   - 问题：缓存清理阶段全量扫描目录在大缓存场景开销高。
   - 方案：引入轻量 metadata（模型目录大小、最后访问时间），优先基于 metadata 做淘汰。
   - 验收：缓存目录 >100GB 时，清理耗时明显下降。

4. **错误结构化输出标准化**
   - 方案：统一输出 `error_type / error_stage / root_cause / short_trace`。
   - 验收：失败任务可自动聚合统计 Top N 错误原因。

### Phase 2（2~4 周，中风险中收益）

5. **配置系统增强（CLI 覆盖复杂类型）**
   - 方案：支持 `json:` 前缀与 `--set-file`，提升 list/dict 覆盖能力。
   - 验收：常见复杂配置可纯命令行覆盖，错误提示可读。

6. **CV Benchmark 参数语义修正**
   - 问题：暴露 `batch_size` 但未实际使用，指标解释有偏差。
   - 方案：实现真实批处理推理，或移除该参数并在文档明确单样本模式。
   - 验收：报告中吞吐与 batch 参数含义一致。

7. **ASR 指标标准化**
   - 方案：新增标准 Levenshtein WER（S/D/I），保留现有近似指标用于兼容。
   - 验收：报告同时显示 `wer` 与 `wer_approx`，并说明计算口径。

### Phase 3（4~8 周，架构升级）

8. **可插拔 Benchmark Runner 架构**
   - 目标：减少重复流程代码（准备、预热、计时、汇总、异常处理）。
   - 方案：抽象统一 Runner 协议，LLM/CV/ASR/Diffusion 只实现任务特有逻辑。

9. **统一指标事件模型（Event Schema）**
   - 目标：实现跨任务统一分析。
   - 方案：定义 `start/end/error/metric` 事件，支持后续实时看板或时序分析。

10. **报告生成管线解耦**
    - 目标：支持 JSON/CSV/HTML 之外的目标输出（如数据库或可观测平台）。
    - 方案：生成器模块化，分离“数据加工”和“渲染层”。

---

## 3. 关键技术改造清单（按模块）

| 模块 | 当前痛点 | 优化动作 | 预期收益 |
|---|---|---|---|
| `tensorforge/llm_bench.py` | TTFT 采样噪声较大 | 按块读取 + 首 token 事件时间戳 | 延迟指标更稳定 |
| `tensorforge/run_suite.py` / 并发模块 | 加载时间混入吞吐 | 冷启动/稳态分离统计 | 性能定位更准确 |
| `tensorforge/model_manager.py` | 缓存扫描开销高 | metadata 增量统计 + LRU 淘汰 | 大缓存场景更快 |
| `tensorforge/collector.py` | 长时采样内存和聚合成本上升 | 窗口聚合/在线统计 | 降低内存占用 |
| `tensorforge/config_manager.py` | CLI 覆盖复杂类型困难 | `json:` + `--set-file` | 配置更灵活可靠 |
| `tensorforge/error_handler.py` | 异常上下文不统一 | 错误分类标准化 | 故障排查效率提升 |

---

## 4. 可量化 KPI（建议）

- **稳定性**
  - 同配置重复 10 次：TTFT 变异系数下降 **20%+**。
- **效率**
  - 典型全量测试总耗时下降 **15%+**（稳态模式）。
  - 缓存清理耗时下降 **30%+**（大模型缓存场景）。
- **可观测性**
  - 失败任务中可自动归类比例达到 **95%+**。
- **资源占用**
  - 长时运行峰值内存下降 **20%+**。

---

## 5. 风险与回滚策略

- **风险 1：指标口径变化导致历史不可比**
  - 对策：保留旧字段，新字段并行输出至少 2 个版本；报告中明确 `metric_version`。

- **风险 2：性能优化影响功能正确性**
  - 对策：优化与业务逻辑解耦，先加开关（默认关闭），灰度启用。

- **风险 3：缓存 metadata 与真实目录不一致**
  - 对策：定期全量校准；异常时自动 fallback 到实时扫描。

---

## 6. 落地执行建议

1. 先从 **Phase 1 的“观测层优化”** 开始（不改模型输出，只改计时和统计）。
2. 每个优化项都采用：
   - 小步提交（可回滚）
   - 补充单元测试 + 回归样例
   - 在报告中增加“口径说明”
3. 每个阶段结束后输出一次对比报告（优化前 vs 优化后）。

---

## 7. 本周可直接启动的任务（建议 backlog）

- [ ] Task-1：重构 LLM TTFT 采样与字段说明。
- [ ] Task-2：并发压测新增 `model_load_s / inference_only_s`。
- [ ] Task-3：模型缓存 metadata 文件原型（含校准逻辑）。
- [ ] Task-4：错误分类标准化与报告聚合。
- [ ] Task-5：配置覆盖增强（`json:` 与 `--set-file`）。

---

如需，我可以按这个方案继续输出下一步内容：
- **版本化实施计划（v0.1/v0.2/v1.0）**
- **每个 Task 的代码级改动点与测试用例清单**
- **可直接导入 issue tracker 的任务模板**
